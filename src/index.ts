// Placeholder entry point. The real engine (data loader, RNG, strategy engine,
// generate()) lands in codesteps 03-06; the Foundry bootstrap in codestep 07.
// This exists so the build/lint/test pipeline (codestep 02) has something to run.
export const ONOMASTICON_VERSION = "0.0.1";
